{
        println("Hello,"+" World!") // A comment
      	
        var pi = 1.0
      
        for var i = 1.0; i < 1000000.0; var i = i + 2.0 {
            var pi = pi - 1.0 / (1.0 + i * 2.0) + 1.0 / (1.0 + (i + 1.0) * 2.0)
        }
        
        println("pi is almost "+string(pi*4.0))
        
        for var i = 10; i > 0; var i = i - 1 println(string(i))
}
